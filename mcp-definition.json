{
  "name": "mstodo",
  "version": "1.0.0",
  "displayName": "Microsoft To Do",
  "description": "Manage Microsoft To Do tasks with MCP",
  "runtime": {
    "target": "stdio",
    "server": {
      "path": "npm",
      "args": ["run", "start"]
    }
  },
  "capabilities": {
    "tasks": {
      "description": "Access and manage Microsoft To Do tasks"
    }
  },
  "tools": {
    "auth-status": {
      "description": "Check if you're authenticated with Microsoft Graph API. Shows current token status and expiration time, and indicates if the token needs to be refreshed."
    },
    "get-task-lists": {
      "description": "Get all task lists available in your Microsoft To Do account."
    },
    "create-task-list": {
      "description": "Create a new task list.",
      "parameters": {
        "displayName": {
          "description": "The name of the new task list.",
          "type": "string",
          "required": true
        }
      }
    },
    "update-task-list": {
      "description": "Update an existing task list.",
      "parameters": {
        "listId": {
          "description": "The ID of the task list to update.",
          "type": "string",
          "required": true
        },
        "displayName": {
          "description": "The new name for the task list.",
          "type": "string",
          "required": true
        }
      }
    },
    "delete-task-list": {
      "description": "Delete a task list.",
      "parameters": {
        "listId": {
          "description": "The ID of the task list to delete.",
          "type": "string",
          "required": true
        }
      }
    },
    "get-tasks": {
      "description": "Get all tasks from a specified task list.",
      "parameters": {
        "listId": {
          "description": "The ID of the task list to retrieve tasks from.",
          "type": "string",
          "required": true
        }
      }
    },
    "create-task": {
      "description": "Create a new task in the specified task list.",
      "parameters": {
        "listId": {
          "description": "The ID of the task list to create the task in.",
          "type": "string",
          "required": true
        },
        "title": {
          "description": "The title of the task.",
          "type": "string",
          "required": true
        },
        "dueDateTime": {
          "description": "The due date and time for the task in ISO format (e.g. 2025-03-31T23:59:59Z).",
          "type": "string"
        },
        "importance": {
          "description": "The importance of the task (low, normal, high).",
          "type": "string",
          "enum": ["low", "normal", "high"]
        },
        "body": {
          "description": "The detailed description of the task.",
          "type": "string"
        }
      }
    },
    "update-task": {
      "description": "Update an existing task.",
      "parameters": {
        "listId": {
          "description": "The ID of the task list containing the task.",
          "type": "string",
          "required": true
        },
        "taskId": {
          "description": "The ID of the task to update.",
          "type": "string",
          "required": true
        },
        "title": {
          "description": "The new title of the task.",
          "type": "string"
        },
        "dueDateTime": {
          "description": "The new due date and time for the task in ISO format (e.g. 2025-03-31T23:59:59Z).",
          "type": "string"
        },
        "importance": {
          "description": "The new importance of the task (low, normal, high).",
          "type": "string",
          "enum": ["low", "normal", "high"]
        },
        "body": {
          "description": "The new detailed description of the task.",
          "type": "string"
        },
        "status": {
          "description": "The status of the task (notStarted, inProgress, completed).",
          "type": "string",
          "enum": ["notStarted", "inProgress", "completed"]
        }
      }
    },
    "delete-task": {
      "description": "Delete a task.",
      "parameters": {
        "listId": {
          "description": "The ID of the task list containing the task.",
          "type": "string",
          "required": true
        },
        "taskId": {
          "description": "The ID of the task to delete.",
          "type": "string",
          "required": true
        }
      }
    },
    "get-checklist-items": {
      "description": "Get all checklist items for a task.",
      "parameters": {
        "listId": {
          "description": "The ID of the task list containing the task.",
          "type": "string",
          "required": true
        },
        "taskId": {
          "description": "The ID of the task.",
          "type": "string",
          "required": true
        }
      }
    },
    "create-checklist-item": {
      "description": "Create a new checklist item for a task.",
      "parameters": {
        "listId": {
          "description": "The ID of the task list containing the task.",
          "type": "string",
          "required": true
        },
        "taskId": {
          "description": "The ID of the task.",
          "type": "string",
          "required": true
        },
        "title": {
          "description": "The title of the checklist item.",
          "type": "string",
          "required": true
        }
      }
    },
    "update-checklist-item": {
      "description": "Update a checklist item.",
      "parameters": {
        "listId": {
          "description": "The ID of the task list containing the task.",
          "type": "string",
          "required": true
        },
        "taskId": {
          "description": "The ID of the task.",
          "type": "string",
          "required": true
        },
        "checklistItemId": {
          "description": "The ID of the checklist item.",
          "type": "string",
          "required": true
        },
        "title": {
          "description": "The new title of the checklist item.",
          "type": "string"
        },
        "isChecked": {
          "description": "Whether the checklist item is checked.",
          "type": "boolean"
        }
      }
    },
    "delete-checklist-item": {
      "description": "Delete a checklist item.",
      "parameters": {
        "listId": {
          "description": "The ID of the task list containing the task.",
          "type": "string",
          "required": true
        },
        "taskId": {
          "description": "The ID of the task.",
          "type": "string",
          "required": true
        },
        "checklistItemId": {
          "description": "The ID of the checklist item to delete.",
          "type": "string",
          "required": true
        }
      }
    }
  }
} 